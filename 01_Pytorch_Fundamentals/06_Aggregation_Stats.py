'''
1. Sum:
   + torch.sum(tensor)
   + tensor.sum()
   
2. Mean:
   + torch.mean(tensor)
   + tensor.mean()
   
3. Median:
   + torch.median(tensor)
   + tensor.median()
   
4. Mode:
   + torch.mode(tensor)
   + tensor.mode()
   
5. Variance:
   + torch.var(tensor)
   + tensor.var()
   
6. STD:
   + torch.std(tensor)
   + tensro.std()
   
7. Min:
   + torch.min(tensor)
   + tensor.min()
   
8. Max:
   + torch.max(tensor)
   + tensor.max()
   
9. Quantile:
   + torch.quantile(tensor)
   + tensor.quantile()
'''